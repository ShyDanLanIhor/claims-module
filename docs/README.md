# Reference Documents

This folder holds the confidential assessment documents (`.docx`) provided by DICEUS.
They are kept locally only and are excluded from version control via `.gitignore`
(see `docs/*.docx`), as they are marked "confidential — for candidate use only".
They are used as reference for the implementation and are not part of the deliverable.
